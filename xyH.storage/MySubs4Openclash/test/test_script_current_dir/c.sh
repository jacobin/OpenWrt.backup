#!/bin/bash
# SCRIPT_DIR=$( cd -- "$( dirname -- "${BASH_SOURCE[0]}" )" &> /dev/null && pwd )

# SCRIPT_DIR=$(dirname "$0")
# echo $SCRIPT_DIR
# source ./c/cc.sh

echo "++Usage: $(basename $BASH_SOURCE) ..."
echo "++$FUNCNAME"
source ./c/cc.sh
